<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CTF Challenge</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      text-align: center;
    }

    .challenge-container {
      background-color: #ffffff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 500px;
    }

    h1 {
      color: #007bff;
      margin-bottom: 30px;
    }

    p {
      margin-bottom: 15px;
    }

    a {
      text-decoration: none;
    }

    a.btn {
      width: 100%;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="challenge-container">
    <h1>Welcome to the MailRoom</h1>
    
    <!-- Image added here below the Login text -->
    <img src="https://i.ytimg.com/vi/3b6C7Og7dHk/sddefault.jpg" alt="Login Image" style="max-width: 300px;">
    <br>
    <br>

    <% if @user %>
      <p>Logged in as: <strong><%= session[:email] %></strong></p>
      <% if @user['admin'] %>
        <p><a href="/admin" class="btn btn-warning">Go to Admin Page</a></p>
      <% end %>
      <p><a href="/logout" class="btn btn-danger">Logout</a></p>
    <% else %>
      <p><a href="/login" class="btn btn-primary">Login</a> or <a href="/register" class="btn btn-secondary">Register</a></p>
    <% end %>
  </div>

  <!-- Bootstrap JS and dependencies -->
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>
